import { ThemeColorsPipe } from './theme-color.pipe';

describe('ThemeColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new ThemeColorsPipe();
    expect(pipe).toBeTruthy();
  });
});

/** Static application metadata (technical config, not editable content). */
export const APP_META = {
  name: 'Rabin R — Portfolio',
  version: '2.0.0',
  author: 'Rabin R',
  locale: 'en',
} as const;
